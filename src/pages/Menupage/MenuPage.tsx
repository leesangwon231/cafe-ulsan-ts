import React from 'react';
import {MenusInfo} from "../../model/menu";


interface MenuPageInfo{
  info : MenusInfo;
}

const MenuPage:React.FC<MenuPageInfo> = ({info}) => {
  return (
    <div>
      
    </div>
  );
}

export default MenuPage;